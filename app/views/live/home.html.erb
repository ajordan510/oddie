<!-- main body for signup page -->
<div id="header">
	<script type="text/javascript">
		console.log('here')
			$(document).ready( function() {
			    $('.subMenu').smint();
			});
	</script>

	
	<h1 id="header-text">SpectaFresh</h1>

	<!--<div class="logo-section">
		<button id="signup_button">Sign up today!</button>
	</div> -->
	<!-- Link without using a form button -->
	<!--<a class = "link-button" href="/live/login"> 
		Already a user? Login here.
	</a> -->


	<div id="container">
		<h1 id="log_h1">Already a user? Login here.</h1> 	
		<% if flash[:bad_login] %>
			<div class = "error_message"><%= flash[:bad_login] %></div>
		<% end %>

		<% if @logged_in_user.nil? %>
		<% else %>
			<% @logged_in_user.errors.full_messages.each do |msg| %>
				<p class="error_message"> <%= msg %></p>
			<% end %>
		<% end %>

		<%= form_tag(:action => :post_login, :method => :post) do %>
			<p>
			<%= label_tag(:login_val, "Email Address")    %> <br/>
			<%= text_field_tag(:login_val)  %> <br/>
			</p>
			<p>
			<%= label_tag(:pass_val, "Password")    %> <br/>
			<%= password_field_tag(:pass_val)  %> <br/>
			</p>
			<p>
			<%= submit_tag "Login", :id=>"login_submit" %>
			</p>
		<% end %>

		<p id="register"> or <%= link_to 'Sign Up Today!', "/live/sign_up/" %> </p>
	</div>


	<h3 class="subtext"> This Week's Performances (insert table) </h3>
	<p><br/></p>
	



</div>
